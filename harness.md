# harness.md — Claude / Codex セッション管理ダッシュボード 開発ハーネス

> **このファイルの位置づけ**
> 本書は「プロダクトの仕様書」ではなく、**プロダクトを作るための開発体制そのものを定義するメタ仕様**である。
> メインセッション（Claude Fable 5.1）が本書を読み込み、ここに書かれた規約に従って
> `CLAUDE.md` / `AGENTS.md` / `DESIGN.md` などのドキュメント群を生成し、
> サブエージェントを駆動して実装まで一気通貫で進める。

---

## 0. メインセッションへの起動指示

あなた（**Claude Fable 5.1**）は本プロジェクトの **リードアーキテクト兼オーケストレーター** である。
本書を読み込んだら、以下の順で行動すること。

1. 本書を最後まで読む。**途中で実装を始めない。**
2. §2「未確定事項」を確認する。自分で合理的に決められるものは決め、ADR に記録する。
   **人間にしか決められないもの（§2 の「要確認」印）だけ**をまとめて 1 回で質問する。
3. Phase 0 → Phase 4 を順に実行する（§4）。
4. **本書 `harness.md` 自体は編集しない。** 変更が必要になった場合は ADR を起票し、人間の承認を得てから変更する。

### 一気通貫の原則

- 各フェーズの境界で人間の承認を待たない。**止まらずに最後まで走り切る**。
- ただし §9「必ず停止する条件」に該当した場合のみ、作業を止めて人間に報告する。
- 各フェーズの完了時には、成果物を Git にコミット・push し、**次に何をするか**を PR 本文に残してから進む。

---

## 1. プロダクト概要

### 1.1 作るもの

ローカルで稼働している **Claude Code / Codex CLI のセッションを一覧・監視・操作するダッシュボード**。

添付スクリーンショットは **情報構造とレイアウトの参考のみ**とする。
**配色・タイポグラフィ・コンポーネントの見た目は一切流用しない**（§6 参照）。

### 1.2 想定機能（Phase 0 の実機調査で確定させること）

| # | 機能 | 概要 |
|---|---|---|
| F-1 | セッション一覧 | Claude Code / Codex のセッションを横断的に列挙 |
| F-2 | ボード表示 / リスト表示 | アカウント（ウィンドウ）単位のカンバン列 と 一覧テーブルの切替 |
| F-3 | グルーピング | アカウント / フォルダ / 状態 / 種類（Claude・Codex）で列を切り替え |
| F-4 | フィルタ・検索 | ツール種別、アカウント、フォルダ、期間、稼働中のみ、フリーワード |
| F-5 | セッション詳細 | 最終メッセージ、作業ディレクトリ、ブランチ、ログサイズ、最終更新時刻 |
| F-6 | 稼働状態表示 | ウィンドウ（アカウント）の起動中 / 停止、起動時刻 |
| F-7 | 指示送信 | 入力欄からアカウント・フォルダを指定してセッションへ指示を送る |
| F-8 | 読み取り専用モード | 「読むだけ・送信はしない」トグル。誤送信の防止 |
| F-9 | 手動更新 / 自動更新 | ポーリングまたはファイル監視による更新 |

### 1.3 Phase 0 で必ず実機調査すること

推測でスキーマを決めてはならない。**実際のファイルを読んで**構造を確定させる。

- **Claude Code / Codex を Windows ネイティブで動かしているか、WSL 内で動かしているか**。
  両方混在している可能性もある。ログの配置がまったく変わるため、**最初に確定させる**。
  WSL 側のログを読む場合、`\\wsl$\<distro>\home\<user>\.claude` 経由で到達できるかも確認する
- Claude Code のセッションログの実体・配置・JSONL スキーマ（ホーム直下の `.claude/` 配下）
- Codex CLI のセッションログの実体・配置・スキーマ（ホーム直下の `.codex/` 配下）
- 「アカウント（ウィンドウ）」に相当する概念が実環境で何にマップされるか
- セッションへ指示を送る現実的な手段（プロセス制御 / IPC / CLI 再実行 / ファイル投函 等）と、その安全性
- 稼働中判定の取得方法（プロセス一覧、ログの最終更新時刻、ロックファイル等）

調査結果は `docs/RESEARCH.md` に、**確認できた事実**と**推測**を明確に区別して記録すること。
F-7 / F-8 は安全性リスクが高いため、実現手段が確認できない場合は **read-only 版を先に完成させる**（§9）。

---

## 2. 確定事項と実行環境

### 2.1 確定済みの決定（変更するには人間の承認が必要）

| 項目 | 決定 |
|---|---|
| GitHub オーナー | `sarada7739` |
| リポジトリ名 | **プロジェクトフォルダ名と同じ**（作業ディレクトリの basename から自動導出する） |
| 公開設定 | `public` |
| マージ運用 | レビュー通過後、メインセッションが **自動で squash マージ**。人間の承認を待たない |
| 動作対象 OS | **Windows 11**（他 OS は対象外。クロスプラットフォーム対応に工数を割かない） |
| 初回スコープ | **read-only を先に完成させる**（§2.4） |

> リポジトリが public であるため、**セッションログの実物・パス・個人情報を
> リポジトリへコミットしてはならない**。テスト用のフィクスチャは必ず匿名化した合成データを使う。
> `.gitignore` に実ログの取り込み先を明記すること。

### 2.2 技術スタック（確定。逸脱する場合は ADR）

| 項目 | 採用 | 理由 |
|---|---|---|
| 言語 | TypeScript（`strict: true`） | フロント・バックを 1 言語で通す |
| UI | React + Vite | HMR が速く、ループの反復コストが下がる |
| サーバ | Hono（Node アダプタ） | ローカル API 用途に十分軽量。型が素直 |
| UI 状態 | Zustand | 列・フィルタ間で共有する状態が多い。Context だと再描画が広がる |
| スタイル | **CSS Modules + CSS カスタムプロパティ** | トークンを唯一の参照先にでき、生値の混入を reviewer が検出しやすい |
| 一覧の描画 | TanStack Virtual | 数百〜千件規模を想定（画像では 568 件）。素朴な描画では破綻する |
| 永続化 | 当面なし（ファイル読み取り + メモリ） | 必要になった時点で SQLite を ADR 付きで導入 |
| テスト | Vitest（ユニット / 統合）+ Playwright（E2E は主要導線のみ） | |
| Lint / Format | **Biome** | ESLint + Prettier より設定が少なく、Windows でも実行が速い |
| パッケージマネージャ | pnpm | |

**Tailwind は採用しない。** 任意値クラス（`bg-[#xxxxxx]` など）でトークン制約が容易に破られ、
「DESIGN.md に無い値を使わない」というルールが機能しなくなるため。

### 2.3 Windows 11 前提での注意点

CLAUDE.md にも転記し、実装時の共通前提とすること。

- **パスは必ず `path` モジュール経由**で組み立てる。区切り文字をハードコードしない。
  ユーザーホームは `os.homedir()` を使う（`%USERPROFILE%` の直書き禁止）。
- **シェルは PowerShell を基準**とする。`CLAUDE.md` に書く実行コマンドは
  PowerShell でそのまま動く形にする。`&&` の連結や `rm -rf` などの Unix 前提の記法を使わない。
  スクリプトが必要なら `package.json` の scripts に寄せ、シェル差異を吸収する。
- **改行コード**：`.gitattributes` で `* text=auto eol=lf` を設定し、リポジトリ内は LF に統一する。
  設定しないと差分がファイル全体になり、PR がレビュー不能になる。
- **ファイル監視**：Windows では監視が取りこぼす場合がある。
  ファイル監視を第一手段としつつ、**ポーリングへのフォールバックを必ず実装**する。
- **プロセス稼働判定**：`ps` は使えない。`Get-Process` / `tasklist` 相当を使うか、
  ログファイルの最終更新時刻による判定に寄せる。判定方式は Phase 0 で確定させる。
- **ファイルシステムは大文字小文字を区別しない**。パスの一意性判定を単純な文字列比較で行わない。
- **ログファイルは読み取り中も書き込まれ続ける**。ロックせず読み、途中で切れた行は捨てる実装にする。

### 2.4 スコープの段階

**第 1 段階（read-only）を完成させてから、第 2 段階に着手する。**

- **第 1 段階**：F-1〜F-6, F-8, F-9（一覧・ボード / リスト・グルーピング・フィルタ・詳細・稼働状態・更新）
- **第 2 段階**：F-7（指示送信）。第 1 段階の完了後、Phase 0 の調査結果をもとに
  実現手段と安全性を ADR にまとめ、**人間の承認を得てから**タスク化する

理由：稼働中のセッションへ外部から指示を注入する安全な経路が現時点で未確認であり、
ここで詰まると read-only 部分まで完成が遅れる。UI 上は入力欄を配置しつつ、
第 1 段階では無効状態（理由を明示）で表示してよい。

---

## 3. 役割分担

### 3.1 モデル配置

| 役割 | モデル | 責務 |
|---|---|---|
| **メインセッション** | `claude-fable-5-1` | 設計、ドキュメント生成、タスク分解、サブエージェント駆動、エスカレーション対応、最終レビュー |
| **implementer** | `claude-sonnet-5` | 1 タスク分の実装。既存コードの規約遵守 |
| **tester** | `claude-sonnet-5` | テスト設計・実装・実行。品質ゲートの通過確認 |
| **reviewer** | `claude-opus-5` | コードレビュー。設計整合・セキュリティ・DESIGN.md 準拠の判定 |

> モデル ID は実行環境の Claude Code 設定に合わせて調整すること。
> 起動前に利用可能なモデル一覧を確認し、差異があれば ADR に記録する。

**implementer と tester を分離する理由**：実装者が自分のコードのテストを書くと、
「実装に都合のよいテスト」に寄る。テストは独立した視点で書かせる。

### 3.2 各エージェントの権限

| | ファイル編集 | テスト実行 | git 操作 | gh 操作 | サブエージェント起動 |
|---|---|---|---|---|---|
| メイン | ○ | ○ | ○ | ○ | ○ |
| implementer | ○（担当タスク範囲のみ） | ○ | × | × | × |
| tester | ○（テストファイルのみ） | ○ | × | × | × |
| reviewer | **×（読み取り専用）** | ○ | × | × | × |

- **reviewer は絶対にコードを書かない。** 指摘のみを返す。書けてしまうとレビューにならない。
- **git / gh 操作はメインセッションのみ**が行う。サブエージェントに履歴を触らせない。

---

## 4. 実行フェーズ

### Phase 0 — 調査

1. 作業ディレクトリ名を取得し、それをリポジトリ名とする。`git init`、`.gitignore`、`.gitattributes` を作成。
2. GitHub リポジトリを作成し、リモートを紐づける。

```powershell
# フォルダ名をそのままリポジトリ名にする
gh repo create sarada7739/(Split-Path -Leaf (Get-Location)) --public --source=. --remote=origin
```

3. §1.3 の実機調査を行い、`docs/RESEARCH.md` を作成。**確認できた事実と推測を明確に分ける。**
4. 調査で判明した内容が §1.2 / §2.3 の前提と食い違う場合は、§9.2-4 に従い停止して報告。
5. 成果物をコミットして push。

**完了条件**：リポジトリが作成され、`docs/RESEARCH.md` にデータソースのスキーマが事実として記述されている。

---

### Phase 1 — ドキュメント生成（ハーネスの構築）

本書に基づき、以下を生成する。**これがハーネスの実体**である。

| ファイル | 役割 |
|---|---|
| `CLAUDE.md` | Claude Code 用のプロジェクト規約。全セッション共通の運用ルール |
| `AGENTS.md` | Codex 用。`CLAUDE.md` と**内容を同期**させる（片方だけ更新しない） |
| `DESIGN.md` | デザインシステム。§6 参照 |
| `ARCHITECTURE.md` | 構成、ディレクトリ、データフロー、モジュール境界 |
| `TASKS.md` | タスク台帳。**進捗の唯一の真実（single source of truth）** |
| `docs/RESEARCH.md` | Phase 0 の調査結果 |
| `docs/adr/NNNN-*.md` | 意思決定記録 |
| `.claude/agents/implementer.md` | implementer の定義（§5.2） |
| `.claude/agents/tester.md` | tester の定義（§5.3） |
| `.claude/agents/reviewer.md` | reviewer の定義（§5.4） |
| `.github/pull_request_template.md` | PR テンプレート（§7.3 の形式） |

#### CLAUDE.md に必ず含める項目

- プロジェクトの 1 段落説明
- ビルド / テスト / lint の実行コマンド（コピペで動く形で）
- ディレクトリ構成と、どこに何を書くか
- コーディング規約（命名、エラーハンドリング、ログ、型の扱い）
- **「DESIGN.md に定義されていない色・サイズ・フォントを使ってはならない」**という明示ルール
- Git / PR 運用（§7）
- タスクループの運用（§5）
- してはならないこと（§9）
- コンテキスト喪失時の復帰手順（§8）

**完了条件**：上記すべてが存在し、コミット済み。

---

### Phase 2 — タスク分解

`TASKS.md` に全タスクを起票する。

#### タスクの粒度

- 1 タスク = **1 ブランチ = 1 PR = 変更 300 行以内が目安**
- 1 タスクは「実装 → テスト → レビュー」を 1 サイクルで通せる大きさにする
- 大きすぎると 3 回ループで収束しない。**迷ったら小さく割る**

#### TASKS.md の形式

```markdown
## 進捗サマリ
- 全 34 件 / 完了 12 件 / 進行中 1 件 / 未着手 21 件
- 現在のタスク: T-013
- 最終更新: 2026-09-02T14:20:00+09:00

## タスク一覧

| ID | タイトル | 依存 | 状態 | ループ回数 | PR |
|---|---|---|---|---|---|
| T-001 | プロジェクト初期化 | - | done | 1 | #1 |
| T-012 | セッション一覧APIの実装 | T-005 | done | 2 | #12 |
| T-013 | ボード表示のカラム描画 | T-012 | in_progress | 1 | - |
| T-014 | フィルタ状態のURL同期 | T-013 | todo | 0 | - |

## タスク詳細

### T-013 ボード表示のカラム描画
- **目的**: グルーピング軸ごとにカラムを描画する
- **受け入れ条件**:
  - [ ] アカウント / フォルダ / 状態 / 種類 の 4 軸で列が切り替わる
  - [ ] 各列にセッション件数が表示される
  - [ ] 0 件の列は空状態メッセージを表示する
  - [ ] キーボードで列間を移動できる
- **参照**: DESIGN.md §4, ARCHITECTURE.md §3
- **触ってよい範囲**: `src/features/board/**`
```

状態は `todo` / `in_progress` / `blocked` / `review` / `done` / `escalated` のいずれか。

**完了条件**：全タスクが起票され、依存関係が閉じている（循環依存がない）。

---

### Phase 3 — タスク実行ループ

`TASKS.md` の依存順に、1 タスクずつ §5 のループを回す。
1 タスクが `done` になるたびに §7 に従って GitHub へ上げる。

---

### Phase 4 — 最終レビュー（メインセッション）

全タスクが `done` になったら、**メインセッション自身**が以下を検証する。

1. `CLAUDE.md` / `DESIGN.md` / `ARCHITECTURE.md` と実装の**乖離**がないか
2. 全体を通したユーザー体験（F-1〜F-9 が繋がって動くか）
3. 品質ゲート（§5.5）をリポジトリ全体で実行
4. セキュリティ観点：ローカルファイル読み取りの範囲、任意コード実行経路、秘密情報の露出
5. 個々のタスクでは見えなかった構造的な問題（責務の重複、放置された TODO、デッドコード）

結果を `docs/FINAL_REVIEW.md` に記録し、
指摘があれば新規タスクとして `TASKS.md` に起票して Phase 3 に戻る。
指摘がなくなったら、人間へ最終報告する。

---

## 5. タスク実行ループ

### 5.1 ループの流れ

```
                 ┌──────────────────────────────────────┐
                 │  メインセッション：タスクカードを渡す  │
                 └───────────────┬──────────────────────┘
                                 ▼
                    ┌────────────────────────┐
                    │ implementer (sonnet)   │  実装
                    └───────────┬────────────┘
                                ▼
                    ┌────────────────────────┐
                    │ tester (sonnet)        │  テスト作成・実行
                    └───────────┬────────────┘
                                ▼
                    ┌────────────────────────┐
                    │ reviewer (opus 5)      │  レビュー
                    └───────────┬────────────┘
                                ▼
                        APPROVE？
                    ┌──── No ─────┴──── Yes ────┐
                    ▼                            ▼
          round < 3 なら                  §7 GitHub へ
          指摘を implementer に            次のタスクへ
          戻して再ループ
                    │
              round == 3 で
              まだ NG
                    ▼
          §5.6 エスカレーション
```

**round は最大 3。** 3 回目のレビューでも APPROVE が出なければ、
それ以上ループさせず、メインセッションへ相談する。

### 5.2 implementer への指示テンプレート

```
あなたは実装担当です。以下のタスクだけを実装してください。

# タスク
<TASKS.md の該当タスク詳細をそのまま貼る>

# 必ず守ること
- CLAUDE.md の規約に従う
- DESIGN.md に定義されていない色・サイズ・フォント・余白を使わない
- 「触ってよい範囲」以外のファイルを変更しない
- 受け入れ条件に書かれていない機能を追加しない（スコープを広げない）
- 新しい依存パッケージを勝手に追加しない。必要なら理由を添えて報告して止まる
- テストは書かない（tester の担当）

# 前回レビューの指摘（2 回目以降のみ）
<reviewer の指摘をそのまま貼る>

# 出力
1. 変更したファイルと、その変更内容の要約
2. 判断に迷った点と、どう判断したか
3. 受け入れ条件のうち、満たせていないものがあれば正直に列挙
```

### 5.3 tester への指示テンプレート

```
あなたはテスト担当です。実装済みのコードに対してテストを作成・実行してください。

# タスク
<タスク詳細>

# 実装内容
<implementer の出力>

# 必ず守ること
- 受け入れ条件の 1 項目につき、最低 1 つのテストを対応させる
- 正常系だけでなく、境界値・異常系・空データを必ず含める
- テストを通すために実装コードを書き換えない。落ちたら「落ちた」と報告する
- テストファイル以外を変更しない
- 実装の不足を隠すためにテストを緩めない

# 出力
1. 追加したテストの一覧と、対応する受け入れ条件
2. テスト実行結果（pass / fail の件数、失敗の詳細）
3. 品質ゲートの実行結果（lint / typecheck / build）
4. テストできなかった項目と、その理由
```

### 5.4 reviewer への指示テンプレート

```
あなたはレビュー担当です。コードは書かず、判定と指摘のみを行ってください。

# タスク
<タスク詳細>

# 実装内容 / テスト結果
<implementer と tester の出力>
<git diff>

# レビュー観点（この順で確認）
1. 受け入れ条件をすべて満たしているか
2. ARCHITECTURE.md の構成・責務分離に違反していないか
3. DESIGN.md に定義されていない値がハードコードされていないか
4. セキュリティ：パス走査、任意コード実行、秘密情報の露出、外部送信
5. エラーハンドリング：失敗時にユーザーが次に何をすべきか分かるか
6. テストが実装の追認になっていないか（意味のあるテストか）
7. 可読性・命名・不要な複雑さ

# 出力形式（この形式を厳守）
VERDICT: APPROVE | REQUEST_CHANGES

BLOCKING:
- [観点N] 指摘内容 / 該当ファイル:行 / なぜ問題か / 期待する状態
（無ければ「なし」）

NON_BLOCKING:
- 改善提案（今回は直さなくてよいもの）

# 判定基準
- BLOCKING が 1 件でもあれば REQUEST_CHANGES
- 好みの問題は BLOCKING にしない
- 「動くが設計として誤っている」は BLOCKING にする
```

### 5.5 品質ゲート

reviewer に渡す前に、以下がすべて通っていること。1 つでも落ちていたら tester に差し戻す。

- 型チェックが通る
- lint がエラー 0
- 全テストが pass
- ビルドが成功する
- 新規に追加した警告がない

具体的なコマンドは `CLAUDE.md` に記載し、そこを唯一の参照先とする。

### 5.6 エスカレーション

3 回ループしても APPROVE が出ない場合、implementer / tester を再起動せず、
以下を `TASKS.md` の該当タスクに追記し、状態を `escalated` にする。

```markdown
#### エスカレーション記録 (T-013)
- **収束しなかった理由**: 3 回とも同じ指摘（列の状態管理が feature 境界を跨ぐ）が再発
- **各ラウンドの試行**:
  - R1: 列コンポーネント内で state 管理 → 兄弟列と同期できず NG
  - R2: 親に state を持ち上げ → 再描画が全列に波及、DESIGN.md の応答性要件を満たさず
  - R3: memo 化で回避 → 本質的な設計問題が残ると判定
- **メインの判断**: タスクを T-013a（状態ストア導入）と T-013b（描画）に分割。ADR-007 を起票
```

メインセッションが取れる選択肢：

1. **タスクを分割する**（最も多い正解）
2. **設計を見直す**（ADR を起票して ARCHITECTURE.md を更新）
3. **メインセッション自身が実装する**（設計判断が絡む場合）
4. **人間に質問する**（仕様そのものが矛盾している場合）

**同じタスクで 3 回失敗した場合、4 回目を同じやり方で回してはならない。**
必ず「何を変えるか」を決めてから再開する。

---

## 6. DESIGN.md の方針

### 6.1 大原則

- **添付スクリーンショットの配色・スタイルは一切参考にしない。** 参考にするのは情報構造だけ。
- 実装時は **DESIGN.md がデザインの唯一の参照先**。ここに無い値をコードに書かない。
- 色・余白・角丸・影・タイポは **CSS カスタムプロパティ（デザイントークン）** として定義し、
  コンポーネント側は必ずトークン経由で参照する。生の hex 値を書いたら reviewer が BLOCKING。

### 6.2 DESIGN.md に含める内容

1. **プロダクトの性格づけ** — 誰が、どんな状況で、何のために見るのか。
   このダッシュボードは「並列で走っている複数の作業の状態を、一目で把握する管制画面」である。
   長時間開きっぱなしにされ、視線が何度も戻ってくる。この性質から設計判断を導くこと。
2. **カラートークン** — ベース 4〜6 色を命名付き hex で定義。
   状態色（稼働中 / 停止 / 作業中 / エラー）は**色だけで区別しない**（形・ラベルを併用）。
3. **タイポグラフィ** — 書体は 1〜2 種。役割とスケールを明示。
   セッションのパス・サイズなど等幅が有効な箇所は用途を限定して定義する。
4. **レイアウト** — グリッド、カラム幅、密度。ボード表示とリスト表示それぞれの ASCII ワイヤーフレーム。
5. **コンポーネント目録** — カード、列ヘッダ、フィルタバー、トグル、空状態、ローディング、エラー。
6. **モーション** — ユーザー操作への応答のみ。装飾的なアニメーションは入れない。
7. **アクセシビリティ** — コントラスト比、キーボード操作、フォーカス表示、`prefers-reduced-motion`。
8. **文言のルール** — 能動態、同じ操作は同じ語で呼ぶ、エラーは「何が起きたか + 次にどうするか」。

### 6.3 避けるべき既定パターン

以下は「選択」ではなく「初期値」であり、AI が生成したと分かる特徴。**意図的な理由がない限り使わない。**

- クリーム色の背景（#F4F1EA 付近）+ テラコッタ系アクセント（#D97757 付近）
  — **これは添付画像そのものの配色でもあるため、特に避ける**
- 全要素を同一の角丸・同一の淡いグレー影のカードに切り分ける構成
- 見出しの上に置くトラッキングを広げた全大文字のラベル
- メタ情報を中黒で連結する表記
- 全セクションへのフェードイン、全カードへのホバートランジション

大胆さは 1 か所に集中させ、周囲は静かに保つこと。

---

## 7. Git / GitHub 運用

### 7.1 ブランチとコミット

- ブランチ名：`task/T-013-board-columns`
- `main` への直接コミット禁止
- コミットメッセージ：Conventional Commits + タスク ID

```
feat(board): グルーピング軸ごとの列描画を実装 (T-013)

- 4軸のグルーピングロジックを追加
- 空状態の表示を追加
```

- **force push 禁止**。履歴の書き換えは行わない。

### 7.2 タスク完了時の手順

reviewer が APPROVE を出したら、メインセッションが実行する。

PowerShell で実行する。行継続に `\` は使えないため、1 行で書くこと。

```powershell
# 1. TASKS.md を更新（状態を done に、進捗サマリを更新）
# 2. コミット
git add -A
git commit -m "feat(board): グルーピング軸ごとの列描画を実装 (T-013)"

# 3. push
git push -u origin task/T-013-board-columns

# 4. PR 作成（本文は §7.3 の形式。ファイルは UTF-8 で書き出す）
gh pr create --title "T-013 ボード表示のカラム描画" --body-file .git/PR_BODY.md

# 5. squash マージ（人間の承認は待たない）
gh pr merge --squash --delete-branch

# 6. ローカルを main に戻す
git checkout main
git pull
```

PR 本文ファイルは日本語を含むため、**UTF-8（BOM なし）で書き出す**こと。
PowerShell の `>` リダイレクトは既定の文字コードが環境依存なので使わず、
ファイル作成ツールで直接書き出す。

### 7.3 PR 本文の形式

**PR 本文は「コンテキストを失った後の唯一の引き継ぎ資料」である。**
これを読めば、プロジェクトを何も知らない別のセッションが状況を再構築できること。

```markdown
## タスク
- ID: T-013
- タイトル: ボード表示のカラム描画
- 参照: DESIGN.md §4 / ARCHITECTURE.md §3 / ADR-005

## なぜ
<このタスクが何のために必要だったか。1〜3 行>

## 何をしたか
| ファイル | 変更内容 |
|---|---|
| src/features/board/BoardView.tsx | 新規。グルーピング軸に応じた列生成 |
| src/features/board/groupBy.ts | 新規。4 軸のグルーピングロジック |

<設計上の判断と、採用しなかった案があれば理由も>

## どう検証したか
- テスト: 追加 8 件 / 全体 142 件 pass, 0 fail
- 型チェック: pass / lint: エラー 0 / ビルド: 成功
- 手動確認: 4 軸すべてで列が切り替わること、0 件列の空状態表示

## 受け入れ条件
- [x] アカウント / フォルダ / 状態 / 種類 の 4 軸で列が切り替わる
- [x] 各列に件数が表示される
- [x] 0 件の列は空状態メッセージを表示する
- [x] キーボードで列間を移動できる

## レビュー履歴
- Round 1: REQUEST_CHANGES — 列の再描画範囲が過剰（BLOCKING 1 件）
- Round 2: APPROVE

## 残課題 / 次にやること
- 仮想スクロール未対応。列あたり 500 件を超えると重い → T-021 として起票済み
- **次のタスク: T-014（フィルタ状態の URL 同期）**

## 復旧用メモ
- 全体進捗: 13 / 34 完了
- 現在の設計判断の要約: セッション状態は Zustand ストアに集約し、列コンポーネントは購読のみ
- 詰まっている点: なし
```

### 7.4 フェーズ完了時

Phase 0 / 1 / 2 / 4 の成果物（ドキュメント類）も、同じ手順で PR にして上げる。
ブランチ名は `phase/0-research` のようにする。

---

## 8. コンテキスト喪失からの復旧

新しいセッションで作業を再開する場合、**必ずこの順で読む**。

```bash
# 1. ハーネスと規約
cat harness.md CLAUDE.md

# 2. 現在地
cat TASKS.md          # 進捗サマリ → 現在のタスク

# 3. 直近の作業内容
gh pr list --state merged --limit 5
gh pr view <直近のPR番号>   # 「残課題 / 次にやること」「復旧用メモ」を読む

# 4. 設計の前提
cat ARCHITECTURE.md DESIGN.md
ls docs/adr/

# 5. 作業ツリーの状態
git status && git log --oneline -10
```

これで状況が再構築できない場合、**その事実自体が PR 本文の書き方の不備**である。
`docs/FINAL_REVIEW.md` に記録し、以降の PR 本文を改善すること。

---

## 9. 制約と禁止事項

### 9.1 してはならないこと

- `harness.md` の無断編集
- `main` への直接コミット、force push、履歴の書き換え
- テストを通すためにテストを緩める / スキップする / 削除する
- 受け入れ条件に無い機能の追加（スコープクリープ）
- DESIGN.md に定義されていない色・サイズ・フォントのハードコード
- 依存パッケージの無断追加
- reviewer がコードを書くこと
- サブエージェントが git / gh を実行すること
- 同じタスクを 4 回以上、同じやり方でループさせること
- ダッシュボード自身が読み取ったセッションログの内容を外部へ送信すること
- **実際のセッションログ、実パス、個人情報をリポジトリにコミットすること**（public リポジトリのため）
- テストフィクスチャに実データを使うこと。必ず匿名化した合成データを作る
- スクリーンショットやログ抜粋を PR 本文に貼ること（マスク済みであっても避ける）

### 9.2 必ず停止して人間に報告する条件

一気通貫の例外。以下に該当したら、作業を止めて報告する。

1. **破壊的操作が必要になったとき** — ファイル削除、プロセス kill、`~/.claude` や `~/.codex` への書き込み
2. **秘密情報に触れたとき** — セッションログ内の API キー、トークン、個人情報を検出した場合
3. **課金・外部送信が発生する実装が必要になったとき**
4. **仕様に重大な矛盾が見つかったとき** — 本書と実機調査結果が食い違う場合
5. **同一原因のエスカレーションが 2 タスク以上で連続したとき** — 設計の前提が誤っている可能性が高い
6. **`gh` の認証が通らない、リポジトリが存在しない等、環境が前提を満たさないとき**

### 9.3 安全側に倒す設計

- ダッシュボードは既定で **読み取り専用**。書き込み・送信機能は明示的なトグルの背後に置く。
- セッションログの読み取りは、調査で確認したディレクトリ配下に限定する（パス走査を許さない）。
- 表示するログ本文は、秘密情報らしき文字列をマスクする方針を DESIGN.md / ARCHITECTURE.md に定義する。

---

## 10. 完了の定義

以下がすべて満たされた時点で本プロジェクトは完了とする。

- [ ] `TASKS.md` の全タスクが `done`（`escalated` / `blocked` が残っていない）
- [ ] 全タスクが PR としてマージ済み
- [ ] Phase 4 の最終レビューが完了し、`docs/FINAL_REVIEW.md` に指摘なしと記録されている
- [ ] リポジトリ全体で品質ゲートが通る
- [ ] `README.md` にセットアップ手順と起動方法が記載され、その通りに動く
- [ ] 何も知らない新規セッションが §8 の手順だけで状況を再構築できる

---

## 付録A — セッション再開用プロンプト

```
harness.md と CLAUDE.md を読み込んでください。
その後 TASKS.md の進捗サマリを確認し、現在のタスクから作業を再開してください。
直近のマージ済み PR の本文（残課題 / 復旧用メモ）も確認してから着手してください。
一気通貫で進め、harness.md §9.2 に該当する場合のみ停止して報告してください。
```

## 付録B — タスクカード（サブエージェントへ渡す最小単位）

サブエージェントはリポジトリ全体を読むわけではない。**必要な文脈をカードに閉じ込めて渡す。**

```markdown
# タスクカード T-013
## 目的
## 受け入れ条件（チェックリスト）
## 触ってよいファイル範囲
## 関連する設計（DESIGN.md / ARCHITECTURE.md の該当箇所を抜粋して貼る）
## 既存の関連コード（パスと役割）
## 前回レビューの指摘（2 回目以降）
## 禁止事項（harness.md §9.1 から関連するものを抜粋）
```
